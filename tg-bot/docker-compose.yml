version: '3.8'

services:
  bot:
    build: .
    container_name: mood-bot
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
